// Google OAuth web (server) client ID. This is the client the backend expects
// id_tokens for (backend/.env GOOGLE_CLIENT_ID) and the one passed to
// GoogleSignin.configure({ webClientId }) in the app. It must match the web
// client ID used by the web app (frontend/.env VITE_GOOGLE_CLIENT_ID).
export const GOOGLE_CLIENT_ID = '739014927949-qravqicds39a7972vk67cqvjd8uvv6nj.apps.googleusercontent.com';